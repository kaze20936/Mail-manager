# Mail Manager 要件定義書

## Why（なぜ作るか）
メールへの返信作業を自動化・半自動化し、返信の手間を大幅に削減する。
重要なメールを見逃さず、素早く対応できる状態を作る。

## Who（誰が使うか）
- 小規模チーム・個人（自分 + 仲間数名）
- メールの量が多く、返信対応に時間を取られている人

## What（主要機能）

### 1. メール自動振り分け
- 送信者（メールアドレス・ドメイン・名前）でカテゴリ判定
- 件名キーワードでカテゴリ判定
- カテゴリ例：仕事依頼 / 請求・支払い / 問い合わせ / 通知・自動送信 / その他

### 2. 仮返信自動生成（ハイブリッド）
- Claude AIがメール本文を読んで返信文を生成
- カテゴリ別テンプレートをベースにAIが肉付け
- 生成した返信をWebUIで確認・編集してボタン1つで送信

### 3. Webダッシュボード（Streamlit）
- ブラウザで開くローカルWebアプリ（http://localhost:8501）
- 未対応メール一覧を表示（送信者・件名・カテゴリ）
- 各メールの元文と生成返信文を並べて表示
- 返信文をその場で編集可能
- 「この1件を送信」「全件まとめて送信」ボタン
- 送信済みはステータスが自動更新される

### 4. Notion連携（サブ管理）
- 送信済みメールの履歴をNotionに記録
- ステータス管理：未対応 / 返信済み

### 5. 定期自動実行
- 設定した間隔（デフォルト：15分おき）で新着メールをチェック
- 処理済みメールは再処理しない（IDで管理）

### 6. 返信文カスタマイズ機能（追加）
- プロフィール（名前・役職・会社名）をWebUIから登録
- 署名テキストをWebUIから登録して返信文末尾に自動挿入
- 文体・口調（丁寧/普通/カジュアル）、長さ（短め/普通/長め）、構成（文章/箇条書き）を選択
- カテゴリごとに口調・長さ・冒頭フレーズを個別設定
- 自分の文体サンプルを登録してAIに学習させる（直接入力・Gmail送信済み自動取得・貼り付け）
- テスト生成ボタンで設定後のプレビューを即確認

## Out of Scope（やらないこと）
- 確認なしの完全自動送信（必ずWebUIで確認してから送信）
- 複数Gmailアカウントの同時管理（まず1アカウント）
- スパムフィルタリング（Gmailの既存フィルタに任せる）
- 添付ファイルの解析
- 機械学習モデルのファインチューニング（プロンプトで対応）

## Success Criteria（受け入れ条件）
- [ ] Gmailから新着メールを取得できる
- [ ] 送信者・件名のルールでカテゴリに振り分けられる
- [ ] Claudeが各メールに対して返信文を生成できる
- [ ] WebUIでメール一覧と返信文が確認できる
- [ ] WebUI上で返信文を編集できる
- [ ] 編集した返信文を保存して後で送れる
- [ ] チェックボックスで複数メールを選択して一括送信できる
- [ ] 名前・役職・会社名・署名をWebUIで登録できる
- [ ] AIが生成する返信文末尾に署名が自動追加される
- [ ] カテゴリごとに口調・長さ・冒頭フレーズを設定できる
- [ ] 文体サンプルをWebUIから登録・削除できる
- [ ] Gmailの送信済みから文体サンプルを自動取得できる
- [ ] テスト生成ボタンで設定の変化をすぐ確認できる
- [ ] プログラミング不要・WebUIだけで全設定が完結する

## 非機能要件
- Python 3.10以上で動作
- APIキーは .env ファイルで管理（コードにハードコードしない）
- 設定はSQLiteに保存（起動するだけで前回設定が復元される）
- 1回の処理で最大50件を上限とする（レート制限対策）
- ログファイルに処理結果を記録する
- 設定UIにはわかりやすいラベル・説明文を必ず付ける
