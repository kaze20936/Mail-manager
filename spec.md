# Mail Manager 仕様書

## 技術スタック
- **言語**: Python 3.10+
- **WebUI**: Streamlit（ローカルブラウザアプリ）
- **Gmail操作**: Google Gmail API v1（`google-api-python-client`）
- **AI生成**: Anthropic Claude API（`anthropic` SDK、claude-sonnet-4-6）
- **Notion管理**: Notion API（`notion-client`）
- **設定管理**: `python-dotenv`
- **データ保存**: SQLite（メール・返信文・ステータス・ユーザー設定をローカル管理）

## アーキテクチャ概要

```
[Gmail受信トレイ]
      ↓ 新着メール取得（Gmail API）
[振り分けエンジン]
      ↓ 送信者・件名ルール判定
[カテゴリ分類]
      ↓
[Claude AI 返信生成]  ← ユーザー設定（プロフィール・文体・サンプル）を注入
      ↓
[SQLite DB保存]    ← メール・返信文・ステータス・ユーザー設定を管理

        ↓ ブラウザで開く
[Streamlit WebUI]
 ┌────────────────────────────────────────┐
 │ 📬未対応 │ 💾保存済み │ ✅送信済み │ ⚙️設定 │
 └────────────────────────────────────────┘
```

## ディレクトリ構成

```
mail-manager/
├── app.py               # Streamlit WebUI エントリーポイント
├── fetch.py             # メール取得・AI生成バッチ処理
├── gmail_client.py      # Gmail API操作（取得・送信）
├── classifier.py        # メール振り分けロジック
├── reply_generator.py   # Claude API呼び出し・返信生成（★設定注入を追加）
├── notion_logger.py     # Notion 送信履歴記録
├── database.py          # SQLite操作（★ユーザー設定テーブルを追加）
├── config.py            # 設定読み込み
├── rules.yaml           # 振り分けルール定義
├── mail_manager.db      # SQLiteデータベース（自動生成）
├── .env                 # APIキー（gitignore対象）
└── logs/
    └── mail-manager.log
```

## 返信文カスタマイズ機能 仕様

### DBテーブル設計

#### `user_settings`（キーバリュー形式）
| カラム | 型 | 内容 |
|--------|-----|------|
| key | TEXT PRIMARY KEY | 設定キー名 |
| value | TEXT | 設定値 |
| updated_at | TEXT | 更新日時 |

設定キー一覧:
- `profile_name` — 名前（例: 田中 太郎）
- `profile_role` — 役職（例: 営業部長）
- `profile_company` — 会社名（例: 株式会社ABC）
- `signature` — 署名テキスト（複数行可）
- `style_tone` — 口調（`丁寧` / `普通` / `カジュアル`）
- `style_length` — 長さ（`短め` / `普通` / `長め`）
- `style_format` — 構成（`文章` / `箇条書き`）

#### `category_styles`（カテゴリ別スタイル）
| カラム | 型 | 内容 |
|--------|-----|------|
| category | TEXT PRIMARY KEY | カテゴリ名 |
| tone | TEXT | 口調（全体設定を上書き） |
| length | TEXT | 長さ（全体設定を上書き） |
| format | TEXT | 構成（全体設定を上書き） |
| opening | TEXT | 冒頭の一言（例: お世話になっております） |
| updated_at | TEXT | 更新日時 |

#### `style_samples`（文体サンプル）
| カラム | 型 | 内容 |
|--------|-----|------|
| id | INTEGER PRIMARY KEY | ID |
| content | TEXT | 自分が書いたメール文（サンプル） |
| memo | TEXT | メモ（どんな場面のサンプルか） |
| created_at | TEXT | 登録日時 |

最大10件まで（古いものから自動削除）

### AIプロンプト設計

```
システムプロンプト:
あなたは {name}（{role}・{company}）の代わりにメールを返信するアシスタントです。

【文体の指示】
- 口調: {tone}（丁寧/普通/カジュアル）
- 長さ: {length}（短め/普通/長め）
- 構成: {format}（文章形式/箇条書き）
- 冒頭フレーズ: {opening}（カテゴリ別）

【文体サンプル（この人の書き方に合わせて）】
---
{sample_1}
---
{sample_2}
---

【メール情報】
カテゴリ: {category}
送信者: {sender_name}
件名: {subject}
本文:
{email_body}

上記の情報と文体指示に基づき、自然な日本語で返信文を作成してください。
- 署名は含めない（自動で付与されます）
- 具体的な日時や金額など確認が必要な部分は [要確認] と記載
```

### 文体サンプル取得方法

#### 方法A: 直接入力
- WebUIのテキストエリアに過去の自分の返信文を貼り付けて登録

#### 方法B: Gmail送信済みから自動取得
- `gmail_client.py` に `fetch_sent_samples(max_results=10)` メソッドを追加
- Gmail API で `in:sent` クエリを実行
- 返信文（本文）を取得してDBに保存

#### 方法C: テキスト貼り付け
- 方法Aと同じ（テキストエリアへの入力）

### WebUI 設定画面構成（⚙️設定タブに追加）

```
⚙️設定タブ
├── 📋 送信者管理（既存）
├── 🚫 返信しないキーワード（既存）
├── 📂 カテゴリルール（既存）
└── 🎨 返信文スタイル（新規追加）
    ├── 👤 プロフィール・署名
    │   - 名前 / 役職 / 会社名（テキスト入力）
    │   - 署名（テキストエリア）
    │   - [💾 保存] ボタン
    │
    ├── 🎨 全体スタイル設定
    │   - 口調（ラジオボタン: 丁寧 / 普通 / カジュアル）
    │   - 長さ（ラジオボタン: 短め / 普通 / 長め）
    │   - 構成（ラジオボタン: 文章 / 箇条書き）
    │   - [💾 保存] ボタン
    │
    ├── 📂 カテゴリ別スタイル
    │   各カテゴリに折りたたみ欄
    │   - 口調・長さ・構成（全体と異なる場合のみ設定）
    │   - 冒頭フレーズ（テキスト入力）
    │   - [💾 保存] ボタン
    │
    ├── 📝 文体サンプル
    │   - サンプル一覧（削除ボタン付き）
    │   - [直接入力で追加] テキストエリア + [＋追加] ボタン
    │   - [Gmailの送信済みから取得] ボタン + 件数指定
    │   - 最大10件（超えた場合は古い順に削除）
    │
    └── 🧪 テスト生成
        - サンプルメール文を入力
        - カテゴリ選択
        - [🤖 テスト返信を生成] ボタン
        - → 現在の設定で生成された返信文をプレビュー表示
```

## エラーケース

| ケース | 原因 | 期待動作 |
|--------|------|---------|
| プロフィール未設定 | 初回起動時 | プロフィールなしでAI生成（問題なく動く）|
| 文体サンプル0件 | 未登録 | サンプルなしで生成（全体スタイル設定のみ反映）|
| Gmail送信済み取得失敗 | 権限不足 | エラーメッセージ表示、手動入力を促す |
| サンプル10件超え | 追加時 | 最古のサンプルを自動削除して追加 |
| テスト生成のAPI失敗 | Claude APIエラー | エラーメッセージをUIに表示 |

## エッジケース
- プロフィールの名前が空 → プロンプトからプロフィール行をスキップ
- 署名が空 → 返信文末尾への挿入をスキップ
- カテゴリ別スタイルが未設定 → 全体スタイル設定にフォールバック
- 文体サンプルが長すぎる場合 → 先頭500文字のみ使用（トークン節約）

## テスト観点
- [ ] プロフィールを保存してアプリ再起動後も残っている
- [ ] 署名が返信文末尾に自動挿入される
- [ ] カテゴリ別スタイルが全体設定より優先される
- [ ] 文体サンプルを追加・削除できる
- [ ] Gmail送信済みからサンプルを取得できる
- [ ] テスト生成でプレビューが表示される
- [ ] サンプル11件目を追加したら最古が削除される
